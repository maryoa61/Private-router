# تغییرات این بازنویسی

خلاصه‌ی چیزهایی که عوض شد و چرا. مرتب‌شده بر اساس اهمیت.

## ۱. امنیت Worker (`worker.js` → v4، `wrangler.toml`)

نسخه‌ی قبل fail-open بود: `ALLOWED_ORIGINS` خالی یعنی «همه‌ی سایت‌ها» و
`ALLOW_HOSTS` خالی یعنی «همه‌ی مقصدها». چون ورکر هدرهای `Authorization`
و `x-api-key` را فوروارد می‌کرد، این ترکیب یک SSRF/open-proxy قابل
سوءاستفاده بود — مخصوصاً اگر `VITE_WORKER_PROXY_TOKEN` در build عمومی ست
شده باشد، چون آن توکن داخل باندل جاوااسکریپت برای همه قابل خواندن است.

تغییرات:

- پیکربندی ناقص حالا **۵۰۳** می‌دهد، نه دسترسی باز. برای توسعه‌ی محلی
  متغیر `DEV_ALLOW_ALL="true"` اضافه شده.
- `ALLOW_HOSTS` یک پیش‌فرض واقعی گرفت (فقط providerهای خود اپ).
- فقط `https` مجاز است (مگر در حالت dev).
- بلاک کردن مقصدهای شبکه‌ی داخلی: loopback، RFC1918، CGNAT و مهم‌تر از
  همه `169.254.0.0/16` (سرویس metadata).
- `redirect: 'manual'` — قبلاً `follow` بود و یک ریدایرکت از هاست مجاز
  می‌توانست allowlist را دور بزند.
- فقط هدرهای whitelist‌شده به مقصد فوروارد می‌شوند (قبلاً هر هدر `x-*`).
- `set-cookie` و چند هدر دیگر از پاسخ حذف می‌شوند؛ در عوض
  `Access-Control-Expose-Headers` اضافه شد تا هدرهای rate-limit قابل
  خواندن باشند.
- مقایسه‌ی توکن واقعاً زمان‌ثابت شد (SHA-256 هر دو طرف، سپس مقایسه‌ی
  بایتی) — نسخه‌ی قبل با `return` زودهنگام روی اختلاف طول، طول توکن را
  لو می‌داد.
- ورکر حالا هر دو قالب `?url=` و `/<encoded>` را می‌فهمد.

## ۲. ذخیره‌نشدن داده‌ها (`utils/storage.ts`, `App.tsx`)

فقط `services` و `settings` در localStorage بودند؛ گفتگوها، Comboها،
الگوهای پرامپت و آداپتورها با هر refresh پاک می‌شدند. حالا همه از یک
helper مشترک استفاده می‌کنند. گفتگوی فعال هم بین refreshها حفظ می‌شود.

`handleWipeData` هم واقعاً همه‌چیز را پاک می‌کند: تنظیمات، شمارنده‌های
`combo_rr_*` و فلگ ورود مدیر (که قبلاً باقی می‌ماندند).

## ۳. تنظیماتی که هیچ کاری نمی‌کردند

- **تم**: `App` یک state جدا داشت، پس سوییچ داخل تنظیمات بی‌اثر بود.
  حالا `settings.theme` تنها منبع حقیقت است و تغییر، فوری اعمال می‌شود.
- **`autoTitle`**: عنوان گفتگو از اولین پیام کاربر ساخته می‌شود.
- **`ttsEnabled`**: دکمه‌ی صدا فقط وقتی فعال باشد نمایش داده می‌شود.
- **`defaultContextLimit`**: گفتگوی جدید از آن استفاده می‌کند (قبلاً ۶
  هاردکد بود).

## ۴. کارت تحلیل کد (`utils/codeAnalysis.ts`)

`ChatView` یک کارت کامل برای `msg.codeAnalysis` رندر می‌کرد که هیچ‌جا
مقداردهی نمی‌شد. حالا پاسخ Code Doctor parse می‌شود و اگر با قالب
موردانتظار خواند، کارت ساختاریافته نشان داده می‌شود؛ وگرنه همان متن
ساده (degrade، نه خطا).

تشخیص کد هم اصلاح شد: رجکس قبلی `\{[\s\S]*\}` هر متن فارسیِ حاوی یک جفت
آکولاد را «کد» می‌دید.

## ۵. UIهای ساختگی که حذف شدند

- **دکمه‌ی «جستجوی وب زنده»**: هیچ کاری نمی‌کرد (یک بلوک کامنت خالی).
- **Endpoint و API Key اختصاصی Combo**: `router.local/...` و
  `cr-live-...` تولید می‌شدند و با دکمه‌ی کپی مثل اعتبارنامه‌ی واقعی
  نمایش داده می‌شدند. این اپ بک‌اند ندارد؛ جایشان یک توضیح صادقانه آمد.
- **دکمه‌های پیوست فایل و میکروفن**: تزئینی بودند.

## ۶. UX چت

- اسکرول خودکار به آخرین پیام.
- Enter برای ارسال، Shift+Enter برای خط جدید، textarea به‌جای input.
- قفل ورودی تا رسیدن پاسخ + جلوگیری از ارسال هم‌زمان در یک گفتگو.
- `key={conversation.id}` روی ChatView تا مقادیر drawer از گفتگوی قبلی
  باقی نمانند.
- TTS: کل متن خوانده می‌شود (نه ۱۵۰ کاراکتر اول)، زبان `fa-IR`، کلیک
  دوم = توقف، و با ترک صفحه قطع می‌شود.
- زمان‌ها به‌صورت ISO ذخیره و موقع نمایش قالب‌بندی می‌شوند (`utils/time.ts`).
  قبلاً رشته‌ی ثابت «هم‌اکنون» ذخیره می‌شد که بعد از بازیابی بکاپ بی‌معنا بود.

## ۷. متفرقه

- **Round Robin**: شمارنده‌ی یکنواخت صعودی ذخیره می‌شود؛ قبلاً مقدار
  از پیش modulo شده ذخیره می‌شد و با تغییر تعداد اعضا چرخش نامتوازن بود.
- **Fusion**: مدل داور دیگر کل تاریخچه‌ی گفتگو را دوباره نمی‌گیرد (متن
  پاسخ‌ها خودش داخل پرامپت داور هست) — صرفه‌جویی قابل‌توجه در توکن.
- **SettingsModal**: با هر بار باز شدن با تنظیمات واقعی همگام می‌شود؛
  قبلاً بعد از Pull/Import مقادیر کهنه نشان می‌داد.
- **`??` به‌جای `||`** برای temperature/topP/contextLimit، چون `0` مقدار
  معتبری است و بی‌سروصدا با پیش‌فرض جایگزین می‌شد.
- شناسه‌ی پیام‌ها دیگر روی `Date.now()` تصادم نمی‌کند.
- `tsconfig.json` حالا `strict: true` دارد.
- وابستگی‌های مرده حذف شدند: `@google/genai`، `express`، `dotenv`،
  `tsx`، `esbuild`، `@types/express`. `metadata.json` هم دیگر ادعای
  `SERVER_SIDE_GEMINI_API` نمی‌کند.

## چیزی که عمداً درست نشد

**قفل `/admin` همچنان یک قفل UI است، نه مرز امنیتی.** `VITE_ADMIN_TOKEN`
داخل باندل عمومی است و هرکس می‌تواند بخواندش. این با بک‌اند قابل حل است،
نه با کد سمت مرورگر — و اپ عمداً بک‌اند ندارد. کامنت‌های `adminAuth.ts`
همین را می‌گویند و دست‌نخورده ماندند.
